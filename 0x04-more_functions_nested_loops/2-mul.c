#include "main.h"

/**
 * mul - multiply two input that is given
 * @a: input one
 * @b: input two
 * Return: the resut of the multiplication, c
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
